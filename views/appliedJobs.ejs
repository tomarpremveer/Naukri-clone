<%- include('header.ejs') %>
<div class ="container py-md-5">
  <%- include('flash.ejs') %>
    <div class="col-lg-12 py-3 py-md-5">
      <p class="display-4">Submitted Applications</p>
      <table class="table table-hover table-striped table-bordered">
        <thead>
          <tr>
            <th scope="col">Job Title</th>
            <th scope="col">Company Name</th>
            <th scope="col">Applied On</th>
            <th scope="col">Posted Date</th>
          </tr>
        </thead>
        <tbody>

      <% if(jobs.length > 0) { %>
        <% jobs.forEach((job)=>{ %>
        <tr>
          <td><%= job.appliedJob[0].title %></td>
          <td><%= job.appliedJob[0].companyName %></td>
          <td><%= job.appliedOn %></td>
          <td><%= job.appliedJob[0].postedDate %></td>
        </tr>
        <% }) %>
        <% }else {%>
<tr colspan="5">
  <p>You have not applied for any job</p>
</tr>
          <% } %>

          
        </tbody>
      </table>
      </div>
</div>

<%- include('footer.ejs') %>